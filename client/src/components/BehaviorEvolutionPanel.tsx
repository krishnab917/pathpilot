import { Sparkles } from "lucide-react";

type Evolution = { completedSimulationCount: number; includedSimulationCount: number; method: string; traits: Array<{ trait: string; label: string; score: number; consistency: "consistent" | "varied"; observations: number }>; evolvingFocus: { title: string; description: string; rationale: string } | null };

export function BehaviorEvolutionPanel({ evolution }: { evolution: Evolution | null | undefined }) {
  if (!evolution) return null;
  return <article className="surface-panel p-4"><div className="flex items-start gap-2"><Sparkles className="mt-0.5 size-4 text-primary" /><div><p className="eyebrow">Evolving learning signals</p><h3 className="mt-1 text-base font-semibold">Across your last {evolution.includedSimulationCount} simulation{evolution.includedSimulationCount === 1 ? "" : "s"}</h3><p className="mt-2 text-xs leading-5 text-muted-foreground">{evolution.method}</p></div></div><div className="mt-4 grid border border-slate-200 sm:grid-cols-3 dark:border-slate-700">{evolution.traits.slice(0, 3).map(trait => <div key={trait.trait} className="border-b border-slate-200 p-3 last:border-b-0 dark:border-slate-700 sm:border-b-0 sm:border-r sm:last:border-r-0"><p className="text-[10px] font-semibold uppercase tracking-[0.12em] text-muted-foreground">{trait.consistency}</p><p className="mt-1 text-sm font-medium">{trait.label}</p><p className="data-value mt-2 text-lg font-semibold">{trait.score}</p><p className="mt-1 text-[10px] text-muted-foreground">{trait.observations} simulation{trait.observations === 1 ? "" : "s"}</p></div>)}</div>{evolution.evolvingFocus && <div className="mt-4 border-l-2 border-primary/60 bg-slate-50 p-3 dark:bg-slate-800"><p className="text-xs font-semibold">Next learning focus: {evolution.evolvingFocus.title}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{evolution.evolvingFocus.description}</p><p className="mt-2 text-[10px] leading-4 text-muted-foreground">{evolution.evolvingFocus.rationale}</p></div>}</article>;
}
