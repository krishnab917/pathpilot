import { Button } from "@/components/ui/button";
import { CheckCircle2, ClipboardList, ChevronRight } from "lucide-react";

export type SimulationReviewItem = { step: number; nodeTitle: string; decisionLabel: string; consequence: { message: string; kind: "learning" | "caution" | "team" | "progress" } | null };

export function SimulationDecisionReview({ career, items, onContinue }: { career: string; items: SimulationReviewItem[]; onContinue: () => void }) {
  return <section className="max-w-3xl space-y-4"><header><p className="eyebrow">Simulation review</p><h2 className="mt-2 text-2xl font-semibold tracking-[-0.04em]">Review your decisions first.</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Before PathPilot summarizes learning signals, review the choices and consequences that shaped this {career} situation.</p></header><article className="surface-panel overflow-hidden"><div className="flex items-center gap-2 border-b border-slate-200 px-4 py-3 dark:border-slate-700"><ClipboardList className="size-4 text-primary" /><p className="text-sm font-semibold">Your decision trail</p><span className="status-tag ml-auto">{items.length} decisions</span></div><ol className="divide-y divide-slate-200 dark:divide-slate-700">{items.map(item => <li key={`${item.step}-${item.nodeTitle}`} className="grid grid-cols-[28px_1fr] gap-3 p-4"><span className="data-value grid size-7 place-items-center border border-slate-200 text-xs text-muted-foreground dark:border-slate-700">{item.step}</span><div><p className="text-[10px] font-semibold uppercase tracking-[0.12em] text-muted-foreground">{item.nodeTitle}</p><p className="mt-1 text-sm font-medium">{item.decisionLabel}</p>{item.consequence && <div className="mt-3 border-l-2 border-primary/60 bg-slate-50 px-3 py-2 text-xs leading-5 text-muted-foreground dark:bg-slate-800"><p className="font-semibold text-foreground">What changed</p><p className="mt-1">{item.consequence.message}</p></div>}</div></li>)}</ol></article><div className="flex flex-wrap gap-2"><Button size="sm" onClick={onContinue}>See final results <ChevronRight className="size-4" /></Button><p className="flex items-center gap-1.5 px-2 text-xs text-muted-foreground"><CheckCircle2 className="size-3.5" />Your choices are already saved.</p></div></section>;
}
